// ═══════════════════════════════════════════════════════════
// Vercel Edge Middleware — ОТКЛЮЧЕНО.
// Раньше здесь стояла Basic Auth на /kiosk.html и /admin.html, но она
// мешала планшету открывать киоск по прямой ссылке. Защита теперь —
// внутренние экраны входа: киоск и админка проверяют пароль через
// /api/admin-login (общий ADMIN_PASS), admin.html — свой PIN.
//
// matcher указывает на несуществующий путь, поэтому middleware
// фактически не срабатывает ни на одном адресе. Файл можно удалить.
// ═══════════════════════════════════════════════════════════

export const config = {
  matcher: ['/__middleware_disabled__'],
};

export default function middleware() {
  return; // ничего не блокируем
}
